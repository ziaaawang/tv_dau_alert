# TV端 DAU 归因分析 & 预估工具 — 使用说明

## 一、工具概览

| 模块 | 功能 | 适用场景 |
|------|------|----------|
| 📊 归因分析 | 自动拆解DAU变化的维度贡献 | 周报异动分析、临时归因 |
| 📈 短期预估 | 天级DAU预测（1-30天） | 月中/月末DAU预估、节假日预判 |
| 📈 长期预估 | 月级DAU预测（1-6个月） | 季度规划、预算评估 |

启动方式：`streamlit run app.py`，浏览器访问 `localhost:8501`

---

## 二、归因分析

### 操作步骤

1. **选择对比模式**（侧边栏）
   - 周环比（WoW）：本周 vs 上周
   - 年同比（YoY）：本周 vs 去年同周
   - 自定义日期：任意两个时间段对比

2. **可选：勾选补充维度**（侧边栏底部）
   - 单维度：启动方式（老）、单端/双栖、城市等级、年龄、性别、内容品类
   - 支持二维/三维交叉组合

3. **点击「开始分析」**，等待SQL执行完成

### 输出内容

| 区域 | 说明 |
|------|------|
| 核心结论 | 整体YoY、各维度Top贡献者、异动分析、可编辑文本可直接复制到周报 |
| 异动探讨 | AI对话，可追问异动原因（如"TCL增长原因是什么"） |
| 单维度归因 | 厂商 / 启动类型 / 活跃分层，各维度的贡献pp、占比变化 |
| 双维度归因 | 厂商×启动类型 等两两交叉，定位增长驱动类型 |
| 三维度归因 | 厂商×启动类型×活跃分层 全叉乘 |

### 核心指标说明

- **贡献pp**：该维度值对整体YoY的贡献（所有维度值的贡献pp之和 = 整体YoY%）
- **贡献度%**：贡献pp占整体YoY的百分比（衡量"贡献了多少份额"）
- **DAU占比%**：该维度值的DAU占总DAU比例
- **贡献度/占比**：>1 表示超预期贡献，<1 表示贡献不足
- **占比变化pp**：结构性变化，正值表示份额扩张
- **结构贡献pp**：因份额变化带来的贡献（占比涨了 → 结构贡献为正）
- **规模贡献pp**：因整体大盘涨跌带来的贡献（大盘涨 → 所有维度都有正的规模贡献）
- 关系：贡献pp ≈ 结构贡献pp + 规模贡献pp，可判断增长是"自身份额扩张"还是"跟着大盘涨"

---

## 三、短期DAU预估

### 操作步骤

1. **设置日期范围**
   - 历史数据起止日：用于计算YoY基准（建议起始日设为年初）
   - 预测起止日：需要预测的日期范围

2. **调整参数**
   - YoY参考天数：取最近N天计算平均YoY（默认14天）
   - YoY手动调整：针对特定日期段覆盖YoY（如商务策略变动、春秋假）
   - YoY周环比趋势：启用后逐周递增/递减YoY（见下方逻辑）

3. **点击「运行预估」**

### 预估逻辑

```
预测DAU = 去年同性质日期的DAU × (1 + 预测YoY%)
```

**三个关键机制：**

#### 1. 日期性质对齐（26年日期 → 25年参考日期）

不是简单减365天，而是按"日期性质"匹配：

| 优先级 | 规则 | 举例 |
|--------|------|------|
| P0 | 春节按农历对齐 | 26年除夕(2.16) → 25年除夕(1.28) |
| P1 | 节假日按名称+偏移 | 26年端午第1天 → 25年端午第1天 |
| P2 | 寒暑假按比例映射 | 26年寒假第10天 → 25年寒假对应位置 |
| P3 | 工作日按连续工作段位置匹配 | 确保"5天工作段的第3天"对齐 |
| P4 | 避免错位对齐 | 26年普通工作日不会对齐到25年的节假日或调休日，反之亦然 |

#### 2. 分周中/周末 YoY

- **周中YoY**：最近N天中工作日的平均YoY
- **周末YoY**：最近N天中周末 + 法定节假日的平均YoY
- 法定节假日（端午/国庆/春节等）统一按周末处理

#### 3. YoY周环比趋势调整（可选）

解决"用固定YoY预测整个月不够精确"的问题：

```
预测YoY = 基础YoY + 斜率 × 距今周数
```

- 斜率：对最近N周（默认4周）的周级YoY做线性回归，得出每周YoY变化量
- 效果：如果近期YoY在逐周上升，预测线也会逐周上调，而非一条平线
- 可手动修改斜率值，或关闭此功能

### 输出内容

- **趋势图**：26年实际 + 预估线 + 25年参考线
- **明细表**：每日预测DAU、对应25年参考日期、YoY%
- **AI预估 vs 人工预估**：AI用自动计算的YoY，人工可手动调整YoY后对比
- **Excel导出**：一键下载

---

## 四、长期DAU预估

### 预估逻辑

基于**留存模型**：

```
预测DAU = 主启DAU（留存累加） + 外唤DAU（近3月均值）
主启DAU = Σ(第i月前新增 × 留存率LT(i))，i = 0~11
留存率LT(n) = a × (n+1)^(-b)  （幂律衰减）
```

- a：近似次月留存率
- b：衰减速度
- 每个厂商独立参数，可在界面上调整

### 操作步骤

1. 点击「获取历史月度数据」
2. 调整各厂商的LT留存参数（a, b）
3. 查看预测结果：月度DAU趋势图 + 分厂商明细 + 主启/外唤拆分

---

## 五、常用操作速查

| 场景 | 操作 |
|------|------|
| 周报归因 | 归因分析 → 周环比 → 开始分析 → 复制结论 |
| 月中预估DAU | 短期预估 → 设置预测到月底 → 运行 → 导出Excel |
| 节假日影响预判 | 短期预估 → 覆盖节假日YoY → 对比调整前后 |
| 厂商策略变动评估 | 短期预估 → 手动调整对应日期段YoY → 查看影响 |
| 季度DAU目标拆解 | 长期预估 → 调整厂商参数 → 查看月度预测 |
